{
  "name": "inputbridge-desktop",
  "version": "0.1.0",
  "private": true,
  "author": "InputBridge",
  "description": "InputBridge desktop screen capture OCR translator for Windows",
  "main": "main.js",
  "scripts": {
    "start": "npm run build:native && electron .",
    "start:app": "electron .",
    "build:ocr": "dotnet publish native/OcrHelper/OcrHelper.csproj -c Release -r win-x64 --self-contained true -o native/OcrHelper/publish",
    "build:speech": "dotnet publish native/SpeechHelper/SpeechHelper.csproj -c Release -r win-x64 --self-contained true -o native/SpeechHelper/publish",
    "build:native": "npm run build:ocr && npm run build:speech",
    "dist": "npm run build:native && electron-builder --win portable"
  },
  "devDependencies": {
    "electron": "43.1.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "com.inputbridge.desktop",
    "productName": "InputBridge Desktop",
    "asar": true,
    "files": [
      "main.js",
      "preload.js",
      "src/**/*",
      "assets/**/*",
      "renderer/**/*",
      "voice-overlay/**/*",
      "selection/**/*",
      "result/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "native/OcrHelper/publish",
        "to": "ocr",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "native/SpeechHelper/publish",
        "to": "speech",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "native/WhisperHelper",
        "to": "whisper",
        "filter": [
          "**/*.py"
        ]
      },
      {
        "from": "assets/tessdata",
        "to": "tessdata",
        "filter": [
          "**/*.traineddata.gz"
        ]
      },
      {
        "from": "../icons/icon128.png",
        "to": "icon128.png"
      },
      {
        "from": "../languages.js",
        "to": "languages.js"
      }
    ],
    "win": {
      "icon": "build/icon.ico",
      "target": [
        "portable"
      ],
      "artifactName": "InputBridge-Desktop-${version}-${arch}.${ext}"
    }
  },
  "dependencies": {
    "tesseract.js": "^7.0.0",
    "ws": "^8.21.1"
  }
}
